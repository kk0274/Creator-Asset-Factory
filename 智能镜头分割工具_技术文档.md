# 智能镜头分割工具 - 技术文档

## 一、产品概述

### 1.1 产品定位
**智能镜头分割工具**是一款面向短视频创作者、MCN机构和剪辑团队的视频素材处理工具，旨在自动化完成视频镜头拆分、AI智能分类、批量命名和素材导出等核心功能，提升素材处理效率。

### 1.2 核心价值
- **效率提升**：自动化处理流程，减少人工干预
- **精准拆分**：帧级精度的场景检测，避免片段重叠
- **智能分类**：基于AI视觉模型的自动内容分类
- **标准化命名**：统一的文件命名规范，便于素材管理

---

## 二、功能介绍

### 2.1 核心功能

| 功能模块 | 功能描述 | 技术实现 |
|---------|---------|---------|
| **视频导入** | 支持多视频批量上传、拖拽上传 | HTML5 File API |
| **场景检测** | 基于内容的镜头边界检测 | PySceneDetect |
| **帧精确切割** | 精确到帧级别的视频分割 | FFmpeg |
| **AI自动分类** | 基于视觉模型的内容分类 | Qwen2.5-VL + Ollama |
| **智能命名** | 日期+达人名称+序号的命名规则 | 自定义算法 |
| **分类管理** | 自定义分类集合、分类集合保存/加载 | SQLite数据库 |
| **训练集管理** | 人工纠错样本收集、产品级隔离 | 文件系统+数据库 |
| **批量导出** | 支持按分类导出、选中导出、全部导出 | 文件系统操作 |

### 2.2 辅助功能

| 功能模块 | 功能描述 |
|---------|---------|
| **二次拆分** | 对已拆分片段进行更细粒度的拆分 |
| **分类修改** | 点击分类标签直接修改片段分类 |
| **产品隔离** | 不同产品使用独立的训练集和分类配置 |
| **历史记录** | 保存产品名称历史，便于快速切换 |
| **实时日志** | 显示处理进度和运行状态 |

---

## 三、技术架构

### 3.1 架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端层 (Frontend)                       │
│  HTML5 + JavaScript + CSS3                                      │
│  - 响应式网格布局 (4面板)                                        │
│  - 文件上传/拖拽支持                                             │
│  - 实时进度展示                                                  │
│  - 分类标签交互                                                  │
├─────────────────────────────────────────────────────────────────┤
│                        后端层 (Backend)                         │
│  FastAPI + Python                                               │
│  - RESTful API 接口                                              │
│  - 任务管理                                                      │
│  - 视频处理调度                                                  │
│  - AI分类协调                                                    │
├─────────────────────────────────────────────────────────────────┤
│                        处理引擎层                               │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐           │
│  │ PySceneDetect│ │   FFmpeg     │ │   Ollama     │           │
│  │ 场景检测     │ │ 视频切割     │ │ AI分类       │           │
│  └──────────────┘ └──────────────┘ └──────────────┘           │
├─────────────────────────────────────────────────────────────────┤
│                        数据存储层                               │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐           │
│  │  SQLite      │ │   文件系统   │ │  LocalStorage│           │
│  │ 任务/分类数据│ │ 视频/训练集  │ │ 产品历史     │           │
│  └──────────────┘ └──────────────┘ └──────────────┘           │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈详情

| 层级 | 技术 | 版本 | 用途 |
|-----|------|-----|-----|
| 前端 | HTML5 | - | 页面结构 |
| 前端 | JavaScript (ES6+) | - | 交互逻辑 |
| 前端 | CSS3 | - | 样式布局 |
| 后端 | Python | 3.10+ | 业务逻辑 |
| 后端 | FastAPI | 0.100+ | API框架 |
| 后端 | Uvicorn | 0.23+ | ASGI服务器 |
| 场景检测 | PySceneDetect | 0.6+ | 镜头边界检测 |
| 视频处理 | FFmpeg | 6.0+ | 视频切割/转码 |
| AI分类 | Ollama | 0.1.30+ | 本地LLM推理 |
| AI模型 | Qwen2.5-VL | 7B | 视觉语言模型 |
| 数据库 | SQLite | 3.0+ | 数据持久化 |

### 3.3 核心模块说明

#### 3.3.1 场景检测模块
- **算法**：基于内容的场景检测（Content-Aware Scene Detection）
- **核心参数**：阈值（10-50），值越低拆分越细
- **输出**：场景边界的帧编号

#### 3.3.2 视频切割模块
- **技术**：FFmpeg帧级精确切割
- **命令示例**：
```bash
ffmpeg -i input.mp4 -vf "select='between(n,start_frame,end_frame-1)'" -vsync 0 -c:v libx264 -preset ultrafast -crf 28 -an -y output.mp4
```

#### 3.3.3 AI分类模块
- **模型**：Qwen2.5-VL:7B
- **输入**：视频关键帧（多帧投票）
- **输出**：分类标签

---

## 四、工作流程

### 4.1 完整处理流程

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  1.导入视频   │───>│  2.场景检测   │───>│  3.视频切割   │
│  批量上传    │    │  PySceneDetect│    │  FFmpeg     │
└──────────────┘    └──────────────┘    └──────┬───────┘
                                               │
                                               v
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  6.导出素材   │<───│  5.分类管理   │<───│  4.AI分类    │
│  按分类导出  │    │  人工纠错     │    │  多帧投票    │
└──────────────┘    └──────────────┘    └──────────────┘
```

### 4.2 详细流程说明

#### 步骤1：视频导入
- 用户选择或拖拽多个视频文件
- 支持格式：MP4、MOV、AVI、MKV
- 前端展示已选择的视频列表

#### 步骤2：场景检测
- 对每个视频进行场景边界检测
- 使用PySceneDetect的ContentDetector
- 可配置检测阈值（10-50）
- 支持帧精确模式（输出帧编号而非时间戳）

#### 步骤3：视频切割
- 根据检测结果进行帧级精确切割
- 使用FFmpeg进行无音频编码
- 输出格式：MP4（H.264）
- 命名规则：`{日期}{达人名称}-{序号}.mp4`

#### 步骤4：AI分类
- 提取每个片段的关键帧（默认3帧）
- 调用Ollama API进行分类
- 使用多帧投票机制提高准确率
- 支持自定义分类列表

#### 步骤5：分类管理
- 用户可点击分类标签修改分类
- 自动将修改记录添加到训练集
- 支持产品级训练集隔离

#### 步骤6：导出素材
- 支持按分类导出、选中导出、全部导出
- 导出目录结构：`输出分类/{达人名称}/{分类}/{视频文件}`

---

## 五、使用步骤

### 5.1 基础使用流程

```
1. 启动服务
   └─ 后端：python main.py (端口8001)
   └─ 前端：python -m http.server 8081

2. 访问前端
   └─ 打开 http://localhost:8081

3. 设置产品（可选）
   └─ 在产品选择器中输入产品名称
   └─ 点击"确定"保存

4. 导入分类（可选）
   └─ 方式A：手动输入分类列表（逗号/换行分隔）
   └─ 方式B：上传分类目录截图，AI自动识别

5. 上传视频
   └─ 点击或拖拽视频文件到素材选择区

6. 开始处理
   └─ 设置拆分阈值（建议25-30）
   └─ 勾选"帧精确模式"
   └─ 点击"开始生成审核片段"

7. 查看结果
   └─ 在片段列表中查看拆分结果
   └─ 点击片段在预览区查看

8. 修改分类（可选）
   └─ 点击片段的分类标签
   └─ 从下拉列表中选择正确分类

9. 导出素材
   └─ 选择要导出的片段
   └─ 点击"导出选中"或"按分类导出"
```

### 5.2 高级功能

#### 5.2.1 分类集合管理
```
1. 设置分类后，输入集合名称
2. 点击"保存"保存分类集合
3. 后续使用时从下拉列表选择加载
```

#### 5.2.2 二次拆分
```
1. 在片段列表中选中一个片段
2. 点击"二次拆分"按钮
3. 输入更细的拆分阈值（建议10-20）
4. 确认拆分
```

#### 5.2.3 训练集查看
```
1. 点击右侧面板的"训练集"按钮
2. 查看各分类的训练样本数量
3. 可清空训练集（谨慎操作）
```

---

## 六、API接口说明

### 6.1 接口列表

| API路径 | HTTP方法 | 功能描述 |
|--------|---------|---------|
| `/api/upload` | POST | 上传视频文件 |
| `/api/process/{task_id}` | POST | 处理视频（拆分+分类） |
| `/api/task/{task_id}` | GET | 获取任务详情 |
| `/api/task/{task_id}/export` | POST | 导出片段 |
| `/api/task/{task_id}/export-by-category` | POST | 按分类导出 |
| `/api/task/{task_id}/scene/{index}/category` | POST | 更新片段分类 |
| `/api/task/{task_id}/re-split/{index}` | POST | 二次拆分 |
| `/api/task/{task_id}/batch-category` | POST | 批量设置分类 |
| `/api/categories` | GET | 获取分类列表 |
| `/api/set-categories` | POST | 设置分类列表 |
| `/api/category-sets` | GET | 获取分类集合 |
| `/api/category-sets/save` | POST | 保存分类集合 |
| `/api/category-sets/load` | POST | 加载分类集合 |
| `/api/set-product` | POST | 设置当前产品 |
| `/api/training-set` | GET | 获取训练集 |
| `/api/training-set/add` | POST | 添加训练样本 |
| `/api/recognize-categories` | POST | 从图片识别分类 |

### 6.2 关键接口示例

#### 处理视频接口
```
POST /api/process/{task_id}
参数：
- skip_ai: 是否跳过AI分类（默认false）
- threshold: 拆分阈值（默认27）
- frame_precision: 是否帧精确（默认true）
- categories: 自定义分类列表（可选）

响应：
{
    "task_id": 1,
    "scene_count": 45,
    "message": "处理完成"
}
```

---

## 七、配置与部署

### 7.1 环境依赖

```bash
# Python依赖
pip install fastapi uvicorn requests scenedetect

# 系统依赖
# FFmpeg: 需要安装并配置到PATH
# Ollama: 需要安装并拉取模型
#   ollama pull qwen2.5vl:7b
```

### 7.2 配置参数

| 参数 | 默认值 | 说明 |
|-----|-------|-----|
| OLLAMA_HOST | http://localhost:11434 | Ollama服务地址 |
| DATA_DIR | ./data | 数据存储目录 |
| TRAINING_DIR | ./data/训练集 | 训练集目录 |
| PORT | 8001 | 后端服务端口 |

### 7.3 启动命令

```bash
# 启动后端服务
cd backend
python main.py

# 启动前端服务（另一个终端）
cd frontend
python -m http.server 8081
```

---

## 八、性能优化策略（已实现）

### 8.1 已优化项

| 优化项 | 优化前 | 优化后 | 预期提升 |
|-------|-------|-------|---------|
| AI并行线程 | 4 | 8 | 50% |
| 分类模式 | 单帧识别 | 多帧投票 | 准确率+20% |
| FFmpeg线程 | 默认 | auto | 30% |
| FFmpeg日志 | 默认 | error级别 | 减少IO |
| 日志输出 | 每个片段 | 每5个片段 | 减少IO |

### 8.2 待优化项

| 优化项 | 当前状态 | 建议方案 |
|-------|---------|---------|
| GPU加速 | 未使用 | 使用GPU版FFmpeg |
| 视频读取 | 重复读取 | 缓存帧数据 |
| 并行切割 | 串行 | 多进程FFmpeg |
| 模型推理 | CPU | GPU推理 |

---

## 九、数据结构

### 9.1 任务数据结构

```python
task = {
    "id": int,                    # 任务ID
    "name": str,                  # 任务名称
    "status": str,                # pending/processing/completed
    "videos": list,               # 视频路径列表
    "output_dir": str,            # 输出目录
    "scenes": list,               # 片段列表
    "logs": list,                 # 日志列表
    "created_at": str             # 创建时间
}
```

### 9.2 片段数据结构

```python
scene = {
    "task_id": int,               # 所属任务ID
    "video_name": str,            # 源视频名称
    "creator_name": str,          # 达人名称
    "scene_number": int,          # 片段序号
    "filename": str,              # 输出文件名
    "path": str,                  # 文件路径
    "start_time": float,          # 开始时间(秒)
    "end_time": float,            # 结束时间(秒)
    "duration": float,            # 时长(秒)
    "category": str,              # 分类标签
    "selected": bool              # 是否选中
}
```

---

## 十、错误处理与日志

### 10.1 错误类型

| 错误类型 | HTTP状态码 | 说明 |
|---------|-----------|-----|
| 任务不存在 | 404 | 指定的task_id不存在 |
| 片段不存在 | 404 | 指定的scene_index无效 |
| 请求超时 | 504 | 处理时间过长 |
| 服务器错误 | 500 | 内部处理异常 |

### 10.2 日志级别

| 级别 | 用途 |
|-----|-----|
| INFO | 正常操作日志 |
| WARNING | 警告信息（如分类不在列表中） |
| ERROR | 错误信息（如处理失败） |

---

## 十一、安全性考虑

### 11.1 安全措施

| 安全项 | 措施 |
|-------|-----|
| 文件上传 | 限制文件类型、大小检查 |
| 路径注入 | 严格路径校验，禁止路径穿越 |
| CORS | 允许所有来源（生产环境应限制） |
| 资源限制 | 限制同时处理的视频数量 |
| 超时控制 | API调用设置超时时间 |

---

## 十二、版本历史

| 版本 | 日期 | 更新内容 |
|-----|-----|---------|
| v1.0 | 2024-06 | 基础功能：视频拆分、AI分类 |
| v1.1 | 2024-07 | 帧精确模式、分类管理 |
| v1.2 | 2024-08 | 训练集系统、产品隔离 |
| v1.3 | 2024-09 | 性能优化、多帧投票 |

---

**文档结束**